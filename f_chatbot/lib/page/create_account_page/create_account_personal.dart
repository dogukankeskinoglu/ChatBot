import 'package:flutter/material.dart';

class CreateAccountPersonal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Create Account Personal"),
      ),
    );
  }
}
